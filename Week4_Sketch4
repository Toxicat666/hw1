function setup() { 
  createCanvas(400, 400);
	colorMode(HSB);
} 

function draw() {
	var h = 0;
  background(0);

 	for(var x=0; x<=height; x+=20){
		for(var y=0; y<=height;y+=20){
			fill(h,100,100);
			h=h+1;
		ellipse(x,y,20,20);
	}
	}
}
