var sizes = [40, 15, 35, 30, 60,20,30,25,80,55];

function setup() {
  createCanvas(600, 600);

  noStroke();

  for (var i = 0; i < sizes.length; i += 1) {
    fill(sizes[i]);
    ellipse(25+i*50, height/2, sizes[i], sizes[i]);
  }
}
